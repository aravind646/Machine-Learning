function Ypredict = TrainHeldOut(Xtrain, Ytrain, testInstanceLabel)
testInstanceLabel = PartitionHeldOut(10, 5);
[m,n] = size(testInstanceLabel);
for i = 1:m
    if testInstanceLabel(i) == 1
        
        
    else
        
    end

nb_train(Xtrain, Ytrain)
end